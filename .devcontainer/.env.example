# Devcontainer shared external volume overrides
# Copy to .env and adjust as needed. These only rename which existing external
# volumes are used. They do NOT create the volumes automatically; run the
# provisioning scripts from the companion automation repo first.

# Prefix you used while provisioning (optional). If you provisioned with a
# prefix, you can either replicate it per-variable below or keep these blank
# and rely on explicit names.
# VOLUME_PREFIX=dev1

# Git config volume (contains global gitconfig, optional gitignore_global)
# Default if unset: git_config
# GIT_CONFIG_VOLUME_NAME=git_config

# SSH keys volume (contains id_rsa/id_ed25519 etc., config, known_hosts)
# Default if unset: ssh_keys
# SSH_KEYS_VOLUME_NAME=ssh_keys

# Corporate / internal certificate bundle volume
# Default if unset: corporate_certificates
# CERTS_VOLUME_NAME=corporate_certificates

# Path inside container where certificate bundle is mounted
# Keep in sync with docker-compose.yml and env vars (SSL_CERT_FILE, etc.)
# CERT_BUNDLE_PATH=/opt/shared/certs/corporate-ca-bundle.pem

# If you need to override the global git config path explicitly you can set
# GIT_CONFIG_GLOBAL in devcontainer.json or Compose. We rely on Compose env now.
# GIT_CONFIG_GLOBAL=/opt/shared/git/.gitconfig
