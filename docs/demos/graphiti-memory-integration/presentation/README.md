# Presentation: Graphiti Memory Integration

Open `index.html` in a browser.

Optional (recommended): serve the folder so in-page links work consistently.
```bash
cd docs/demos/graphiti-memory-integration/presentation
python -m http.server 8000
```
Then open `http://localhost:8000`.

